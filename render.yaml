services:
  - type: web
    name: codeforces-mcp-server
    runtime: node
    buildCommand: cd mcp && npm install && npm run build
    startCommand: cd mcp && node dist/server.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
